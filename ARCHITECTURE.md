# OAP — Operations · Analytics · Performance  
### (OAP – Flight & Operations Analytics Platform)

وثيقة المعمارية الرسمية لنظام **OAP**  
هذه الوثيقة تصف كيف يعمل النظام، ولماذا تم اختيار هذه المعمارية، وما هي القرارات التقنية المعتمدة.

---

## 🎯 أهداف النظام

OAP صُمّم ليكون:

- سريع
- آمن
- قابل للتطوير
- سهل الصيانة
- واضح من حيث المسؤوليات (كل منصة لها دور محدد)

ويهدف إلى:

1️⃣ تحليل العمليات التشغيلية  
2️⃣ دعم اتخاذ القرار  
3️⃣ توفير تجربة ذكاء اصطناعي آمنة  
4️⃣ حفظ البيانات بشكل منظم وقابل للرجوع إليه

---

## 🧱 نظرة عامة على المعمارية

المسار الرئيسي للتطبيق:

User → Cloudflare Pages (Frontend)
↓
Cloud Run (oap-backend / APIs + AI)
↓
Supabase (Database + Auth)

yaml
نسخ الكود

✔ الواجهة ثابتة (Static)  
✔ الذكاء الاصطناعي والخدمات الحساسة داخل Backend  
✔ قاعدة البيانات منفصلة ومحمية

---

## 🔹 المكوّنات الرئيسية

### 1) Cloudflare Pages — Frontend
- يستضيف الواجهة الرسمية
- الدومين: https://oap-kas.com

الملفات الأساسية:
- index.html
- Login.html
- AI.html
- config.js

> لا يوجد أي منطق ذكاء اصطناعي داخل المتصفح — فقط استهلاك API.

---

### 2) Google Cloud Run — Backend + AI
الخدمة: **oap-backend**

المسؤوليات:

- إدارة طلبات الذكاء الاصطناعي
- الاتصال بنماذج Gemini
- قراءة/كتابة البيانات من Supabase
- حماية المفاتيح داخل Secrets
- فرض القواعد الأمنية

قاعدة ذهبية:

> كل الطلبات الحساسة تمر عبر Cloud Run فقط.

---

### 3) Supabase — Database + Auth
المسؤوليات:

- تسجيل الدخول وإدارة المستخدمين
- الجداول (Operations, Logs, Chat History…)
- سياسات أمان RLS
- تخزين ملفات خفيفة

❌ لا يتم تشغيل ذكاء اصطناعي داخل Supabase  
✔ فقط بيانات + صلاحيات.

---

### 4) GitHub — مستودعات الكود

- oap-frontend
- oap-backend

GitHub هو **مصدر الحقيقة للكود** — وليس للاستضافة.

---

## 🔐 الأمن (Security Model)

- المفاتيح السرّية لا توضع في الواجهة
- Cloud Run يستخدم SECRETS
- Supabase محمي بسياسات RLS
- أي Endpoint حساس يمر عبر Backend

❌ ممنوع:
- استدعاء Gemini مباشرة من المتصفح
- حفظ مفاتيح داخل JavaScript
- فتح API بدون Auth

---

## 📜 سجل القرارات المعمارية (ADR)

### ✔ قرار: اعتماد 4 منصات أساسية

تم تثبيت المعمارية على:

- Cloudflare Pages
- Cloud Run
- Supabase
- GitHub

### ❌ قرارات تم إلغاؤها

تم إزالة ودعم الأسباب:

- **GitHub Pages**  
  ليس منصة استضافة رسمية — تسبب ازدواجية.

- **Cloudflare Workers مرتبط بالدومين**  
  كان يتدخل في مرور الطلبات — تمت إزالته.

- **Railway**  
  تم حذف الحساب — لمنع تشغيل خدمات موازية.

- **Render**  
  تم حذف الحساب — لتثبيت مسار استضافة واحد فقط.

> الهدف: منع أي منصة إضافية تؤدي للفوضى أو تضارب المسارات.

---

## 🌐 الهوية & التسمية

الاسم الرسمي:

**OAP — Operations · Analytics · Performance**

الاسم التسويقي (SEO):

**OAP – Flight & Operations Analytics Platform**

يُستخدم في:

- العناوين
- الوثائق
- محركات البحث

---

## 📌 قواعد ثابتة للمستقبل

✔ إذا احتجنا منصة جديدة → يجب توثيق القرار في هذا الملف  
✔ أي تغيير في المعمارية يجب أن يكون **مبررًا ومكتوبًا**  
✔ لا تُعاد إضافة المنصات المحذوفة بدون سبب قوي + توثيق

---

## ✅ حالة النظام الآن

النظام مستقر على:

- Frontend → Cloudflare Pages
- Backend → Cloud Run
- Database/Auth → Supabase
- Code → GitHub

وتم تنظيف كل المنصات القديمة.

> هذه الوثيقة هي المرجع الرسمي — وأي تغيير يجب أن يمر عبرها.

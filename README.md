# oap-backend

الـ Backend الرسمي لمشروع **OAP**  
يشغَّل على **Google Cloud Run** ضمن الخدمة:

- `oap-backend`

---

## 🧱 المعمارية الرسمية للنظام

هذه هي المنصات الوحيدة المسموح استخدامها في المشروع:

1. **Cloudflare Pages** – الواجهة (Frontend)
   - تشغّل الصفحات:
     - `index.html`
     - `Login.html`
     - `AI.html`
     - `config.js`
   - تخدم الدومين الرسمي:
     - `https://oap-kas.com`

2. **Google Cloud Run** – الذكاء الاصطناعي + الـ APIs
   - الخدمة الرئيسية: `oap-backend`
   - يتصل بـ Gemini / نماذج الذكاء الاصطناعي
   - يتعامل مع الملفات الكبيرة والتحليل
   - يحمي المفاتيح السرّية داخل Secrets
   - الواجهة لا تتصل بالذكاء مباشرة — فقط عبر Cloud Run.

3. **Supabase** – قاعدة البيانات + Auth
   - إدارة المستخدمين
   - الجداول
   - Chat History
   - سياسات الأمان (RLS)
   - لا نضع فيه أي منطق ذكاء اصطناعي.

4. **GitHub** – مستودعات الكود فقط
   - `oap-frontend`
   - `oap-backend`
   - النشر يتم آليًا من GitHub إلى Cloudflare Pages / Cloud Run.

---

## 🔁 تدفّق العمل (Flow)

1. المستخدم يفتح: `https://oap-kas.com`
2. يسجّل الدخول عبر Supabase.
3. يدخل إلى صفحة `AI.html`.
4. الواجهة ترسل الطلب إلى **Cloud Run (oap-backend)**.
5. Cloud Run:
   - يتصل بـ Gemini.
   - يقرأ/يكتب من Supabase عند الحاجة.
6. النتيجة تُعرض للمستخدم في الواجهة.

---

## 🧹 التنظيف والمعمارية

تم إلغاء/إزالة كل المنصّات التالية من النظام:

- GitHub Pages كنظام استضافة (تم تعطيله في المستودعات).
- Cloudflare Workers المرتبطة بالدومين `oap-kas.com` (تم حذف جميع الـ Routes والمتغيّرات السرّية).
- Railway (تم حذف الحساب بالكامل).
- Render (تم حذف الحساب بالكامل).

> **قاعدة ثابتة:**  
> ❌ لا نربط الذكاء الاصطناعي مباشرة بالمتصفح.  
> ✔ كل الطلبات تمر عبر **Cloud Run → oap-backend** فقط.  
> ❌ لا نضيف منصات جديدة بدون تحديث هذا الملف.
